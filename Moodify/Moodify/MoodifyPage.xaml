<?xml version="1.0" encoding="utf-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:Moodify.Models" 
    x:Class="Moodify.MoodifyPage">
    <ContentPage.Padding>
         <OnPlatform x:TypeArguments="Thickness"
            iOS ="0,20,0,0"
            Android ="0,40,0,0">
        </OnPlatform>
    </ContentPage.Padding>
    
    <ContentPage.Content>
        <ListView ItemsSource="{x:Static local:SongData.AllSongsGrouped}"
                GroupDisplayBinding="{Binding Title}"
                GroupShortNameBinding="{Binding ShortName}"
                IsGroupingEnabled="true"
                ItemSelected="Handle_ItemSelectedAsync"
            >
            <ListView.GroupHeaderTemplate>
                <DataTemplate>
                    <TextCell Text = "{Binding Title}" TextColor="Black"/>
                </DataTemplate>
            </ListView.GroupHeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding SongName}" Detail="{Binding Genre}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
